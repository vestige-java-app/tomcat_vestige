# Create the image with

`./build`

# Run with 

`docker run -p 8080:8080 -d gaellalire:tomcat-10.0.2`

# Test with 

open a browser to localhost:8080

# Stop with

docker stop <container_id_shown_by_docker_run>
